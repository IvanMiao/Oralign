# PRD：Oralign

- 版本：0.3
- 日期：2026-09-15
- 状态：产品方向已收敛，声音能力与学习交互待真实样本验证
- 产品阶段：MVP / 声音能力与核心体验验证

## 1. 产品摘要

Oralign 直接聆听用户的口语，找到一个值得练习的声音细节，通过原音、示范和即时重说，帮助用户听出区别，并逐步说出自己想要的表达。

核心体验：**说一段 → 听原音 → 听区别 → 改一个动作 → 自己说 → 听具体变化**。局部练习后，用户可以把这个动作带回前后句或整段表达。

长期价值是让真实交流更清楚、从容、轻松，同时保留用户自己的口音和表达方式。当前 MVP 首先要做到：**系统听得有依据，用户练得明白，变化听得出来。**

产品机会在于把用户刚刚说出的任意一段声音，变成围绕本人表达、可以听见证据和变化的微型口语课。准确转写、漂亮改写或流畅的参考语音，都不能单独实现这一价值。

### 本版决策与适用范围

- 普通产品只有一条练习流程；移出研究模式、三个意图槽、真人标注表、JSON 导出和独立 Judge 面板。
- 产品开发优先投入声音判断质量、原音回听、针对性示范、局部重说和目标变化反馈。
- 保留轻量的内部音频样本检查与版本回归，不建设独立研究产品；系统性学习效果研究后移。
- 本文描述目标行为，不表示现有代码已经具备或验证了这些能力。与旧版 discovery、roadmap 或工作台说明冲突时，当前产品范围以本版 PRD 为准；旧材料保留为历史背景。

## 2. 问题、用户与工作假设

### 核心问题

对已经能用外语交流的人，知道一句话可以怎样改写，不等于知道自己刚才是怎样说的，更不等于知道该怎样练。

当前要解决的三个具体障碍：

1. 用户不知道哪一处声音值得注意，难以从整段录音中找到重点。
2. 反馈描述了发音、重音或节奏问题，但用户听不出对应区别。
3. 用户照着建议重说后，不知道刚才练的动作是否改变，也不知道怎样带回自然表达。

### 首发用户假设

- 中文母语、英语约 B1–C1 的成年人，已能连续表达一个完整意思。
- 经常在工作或生活中使用英语，希望表达更清楚、更从容。
- 愿意用约 3–5 分钟处理自己一段录音中的一个重点。
- 首版采用移动端优先的 Web 产品，支持本人单人英语录音；现有本地上传作为同等输入路径保留。

工作更新可以作为录音提示和开发样本来源，不再要求所有录音包含进展、阻塞、请求，也不强制限定为工作场景。首发人群、练习时长和复用动机仍是需要在使用中核对的假设。

暂不优先服务无法连续表达基本意图的初学者、只追求考试分数或消除口音的人，以及需要临床语言治疗的人。

### 用户任务

> 当我说完或准备说一段英语时，我想听见一个值得调整的地方，知道可以怎样试，并在自己的重说中听见变化，而不用先理解一整页批改。

用户也可以主动希望“这里强调得更明确”或“这段说得更从容”。这类表达练习不以原表达错误或造成误解为前提。

## 3. MVP 目标与产品原则

### 三个能力目标

1. **听见：** 从真实声音中识别并定位具体、可回听核对的现象，能够利用转写不能表达的信息。
2. **教会尝试：** 帮助用户感知区别，并将反馈变成一个立即可做的动作。
3. **跟随变化：** 重说后围绕同一目标判断发生了什么；证据不足时提供可靠回听并说明无法判断。

局部声音变化、表达更易理解、形成长期学习效果是不同层次的结果，不相互替代。

### 产品原则

- **声音是判断依据。** 字幕用于解释和导航，不能替原音证明发音、停顿或重音问题。
- **先让用户听见，再解释。** 首屏优先原音与一个重点，完整转写和补充说明按需展开。
- **一次练一个动作。** 一轮重说围绕当前目标，不反复追加新问题。
- **最小必要改变。** 能通过停顿、重音或连贯表达练习时，优先保留原词句。
- **保留原意与身份。** 不奖励单一口音、音色或标准化表达；不为使句子顺畅而补写事实。
- **区别观察、解释和偏好。** “这里有一次停顿”是观察，“可能切断意群”是解释，“我想更从容”是用户目标。
- **可以没有建议，也可以结束。** 用户不必重说、接受建议或获得正向评价才能完成一次使用。
- **让不确定性可理解。** 用具体原因说明限制，不向用户展示未经校准的百分比或复杂证据等级。

## 4. 核心体验

### 4.1 说一段

- 打开即可录音或选择本地本人录音，无必填文字、模式选择或研究表单。
- 可选一句话表达目标；不填写也能完成流程。目标描述不能当作音频已传达该含义的证据。
- 推荐原始录音 30–60 秒，硬上限 180 秒；录制到上限自动停止。短于推荐时长但包含完整、可分析意思的片段仍可接受。
- 本地上传使用同一时长上限，默认大小上限 12 MB；检查前说明限制，不静默截断。无法可靠读取或判断的音频进入错误流程。
- 上传前可回放或重新录制；分析中显示真实进度，保留取消和重试入口。

### 4.2 找到并听见一个重点

- 系统结合整段背景选出 0–3 个可练习建议，默认只展开最值得先练的一项；内部候选不全部暴露给用户。
- 每个建议首先提供原音片段、一句具体观察和一个练习动作。
- 默认片段是包含必要上下文的最小完整意思。用户可一键“多听前后一句”，无需先手动裁切。
- 支持正常速度、慢速和循环回听；字幕随播放高亮，文字可辅助定位。声音判断与前后比较均基于未经变速的录音。
- 清楚区分“可能影响理解或增加费力”与用户主动选择的表达练习。后者不显示为错误。

### 4.3 听区别

- 对声音练习提供针对当前动作的参考示范；尽量使用原词句，仅改变需要展示的停顿、重音或发音细节。
- 让用户便捷切换原音和示范，听出这次要尝试的区别，不要求先阅读长解释或回答测试题。
- 示范是一种选择，不要求用户模仿参考声音的音色或整套语调。
- 用词或组织建议如需改写，明确这是内容表达调整，不能冒充从声音中听出的诊断。
- 参考生成失败或没有可靠呈现目标差异时，不把流畅音频当作有效示范。说明限制，保留原音与动作提示，允许继续尝试或结束。

### 4.4 改一个动作，自己说

- “试一下”直接进入当前片段的重录，默认保留原句、动作提示和示范入口。
- 局部重说推荐 5–20 秒，硬上限 40 秒；范围不足时提示扩展上下文，不强制省略原意。
- 允许用户用自己的措辞，不要求逐字跟读。隐藏文字提示是一个可选动作。
- 在当前目标下可重复尝试；每次只替换本次重说，保留原录音作为基准。
- 切换目标或更换作为基准的原始录音后，清除不再适用的重说比较和结论，防止旧结果挂在新内容上。

### 4.5 听具体变化

- 原片段与重说在同一位置便捷回放，不要求进入独立评分页面。
- 比较优先回答“刚才练的动作发生了什么变化”，例如“这次动词后面的停顿消失了”，不默认输出笼统的“更清楚”。
- 内部结果区分目标变化符合方向、无明显变化、偏离目标方向、无法判断；用户看到具体观察及相关回听入口，不看到总分。
- 目标变化符合方向也不自动证明整体理解改善。不能因语速更快、停顿更少、音色更悦耳就奖励重说。
- 若重说改变了数字、否定、主体或其他重要意思，先指出内容不同，暂不直接判断改善；允许用户说明这是有意改动或再录一次。
- 判断不可靠时明确说明原因，仍允许用户自行回听、再次尝试或结束。每次尝试不强制产生新建议或积极结论。

### 4.6 带回上下文，或结束

- 局部练习后可选择“连着前后句说”，将同一动作带回自然表达；只检查原目标在新上下文中的表现。
- 仅当问题涉及整体组织或用户主动选择时，邀请整段重说；复用录音组件和 180 秒上限，保留原录音及当前目标作为基准，不强制所有用户重录整段。
- 用户随时可结束。总结保留本次目标和有依据的变化；未重说或无法判断时，不声称完成改善。
- 反馈入口保持轻量，例如“听不出区别”“建议不准确”；不要求用户进行真人标注。

## 5. 局部与整段的边界

**整段负责理解背景，局部负责掌握动作，上下文负责尝试迁移。** 这是同一流程中的范围变化，不是两套用户模式。

| 练习对象 | 默认范围 | 何时扩大 |
|---|---|---|
| 具体词语或词边界含混 | 包含该词的词组或短句 | 单词脱离上下文无法判断时 |
| 重音、停顿与意群 | 一个完整意群或短句 | 强调对象或句法关系依赖前后句时 |
| 指代、用词或局部逻辑 | 足以恢复含义的前后句 | 缺少上下文、无法守住原意时 |
| 整体信息组织 | 整段 | 用户选择练习整体组织时 |

- 局部比较只检查该范围与当前目标，不评价缺失的整段信息槽。
- 录音片段不得切断关键声音或使原意失去上下文；定位不可靠时扩大到可用范围，仍不可靠则放弃该建议。
- 自动拼接修改片段不作为 MVP 能力，也不能作为用户已掌握整段表达的证据。
- “隐藏提示再说”“连着前后句说”提供迁移机会，不能单凭一次成功声称长期掌握。

## 6. 声音能力范围与反馈要求

### 首批能力候选

优先在真实样本上检查以下能力，选择最可靠的一至两类进入首版默认反馈；其他能力可关闭，不承诺同时达标。

| 能力 | 需要听到的证据 | 不允许的推断 |
|---|---|---|
| 意群与停顿 | 停顿实际位置及其与句法、意义单元的关系 | 仅凭 ASR 词间间隔或停顿时长判错 |
| 关键词重音 | 实际突出的词与上下文或用户表达目标的关系 | 从词序猜重音，或把一种强调方式视为唯一正确答案 |
| 词语、发音或词边界含混 | 具体可听辨的模糊处及可能混淆 | 从拼写、转写错误推断音素错误或编造舌位指导 |

用词和组织反馈作为辅助能力，明确其内容分析依据，不能掩盖声音能力尚未成立的事实。无可靠声音证据时，不自动转为一页语法或措辞批改。

### 每条建议必须回答

1. **在哪里：** 能播放的原音范围及必要上下文。
2. **听到了什么：** 具体观察，不能只有“发音不自然”等评价。
3. **为什么值得练：** 可能的理解影响，或用户选择的表达目标。
4. **怎么试：** 一个可立即重复的动作；需要示范时，示范应呈现这个动作。

“模型认为如此”或输出中声明 `audio` 来源，不足以证明诊断正确。质量必须通过实际回听与声音对照核查。

## 7. 技术方向与关键契约

### 产品要求与实现假设

已确定的产品要求是：声音诊断必须依赖可核对的声音信息，转写错误不应主导反馈。无法通过黑盒 API 证明模型内部没有文字表征，也不要求界面禁止文字。

优先试验以下路径，并与当前音频和转写联合输入方式对照；分阶段调用是否成为默认实现，取决于质量、延迟和成本实测：

1. **独立听音：** 不提供完整转写、参考答案或改写建议，先记录可恢复的意思、不确定处和声音候选。
2. **定位与复听：** 转写、词时间线和音频工具帮助定位，裁切时保留上下文，核对候选是否存在。
3. **形成练习：** 将声音观察、可能影响和一个动作组织成用户可理解的反馈。
4. **跟随重说：** 围绕原目标检查新的声音；不得以参考句的文字内容替代实际重说的证据。

这些是处理职责，不要求拆成多个 Agent。Agent 的价值在于按需裁切、复听、扩大上下文和核对变化；采用满足质量要求的最简单编排。

供应商经适配接口调用。现有 Gemini、Scribe 和 ElevenLabs 参考语音可以作为起点，具体模型、提示版本及费用留在工程配置和实测记录中；支持音频输入不等于支持可靠口语教学。

### 关键数据契约

| 对象 | 必要内容与约束 |
|---|---|
| 练习目标 | 稳定标识、目标来源（系统建议/用户选择）、能力类型、具体观察、影响或表达目标、单一练习动作 |
| 声音证据 | 原录音标识、可播放范围、上下文范围、实际使用的证据来源及不确定原因；时间必须在真实音频内 |
| 参考示范 | 对应目标、示范文本或内容、要呈现的声音变化、可用状态；不能仅以生成成功判定示范有效 |
| 重说比较 | 原目标标识、两个音频版本、比较范围、原意状态（保留/改变/不确定）、目标变化方向、具体观察或无法判断原因 |
| 版本记录 | 模型、提示、分析与比较版本；普通日志不包含音频或完整转写 |

数字、日期、否定、责任主体、不确定性和请求强度必须受到保护。原意含混时不编造替代表达，可保留与含混内容无关的声音练习，或仅针对必要之处请求澄清。

输出必须经过结构与定位校验；无效候选不得直接展示。单项失败应尽可能保留其他有效建议和原音回听。

## 8. MVP 功能范围

### P0 — 完成一次有声音依据的练习

- 零必填录音与本人音频上传、质量检查、回放和重录。
- 整段背景分析、0–3 条建议、首屏一个重点。
- 原音准确定位、正常/慢速/循环回听、同步字幕及上下文扩展。
- 至少一类经实际样本检查的声音反馈，配一个具体练习动作和针对性声音示范。
- 一键局部重说、同一目标下的重复尝试、原音与重说便捷比较。
- 对已验证能力提供目标变化观察；无法判断时有可用的回听降级路径。
- 可选隐藏提示、连着前后句说；整段重说复用已有录制能力，不另建整段评分系统。
- 无建议、证据不足、定位失败、音频或供应商失败等分支；轻量反馈和结束会话。
- 当前会话数据清理，以及轻量内部音频检查与版本回归。

参考生成、声音定位和目标变化识别均属于核心质量。不能仅靠正常录音、展示文案和播放任意参考音频，就宣称完成声音练习 MVP。

### P1 — 核心闭环可用后

- 根据“听不出区别”切换另一种提示或更明显的对照示范。
- 从已验证的声音类型扩展更多能力。
- 跨会话练习记忆、个人目标和间隔复习；另行定义保存授权与数据期限。
- 基于真实使用探索更多场景提示，以及更自然的语音引导。

### 不进入当前 MVP

- 用户研究模式、三个意图槽、真人标注表、JSON 导出、独立 Judge 面板。
- 专门的研究平台、标注市场、大规模多听者盲测和长期学习效果证明。
- CEFR、母语程度、口音或综合能力总分；全面逐句纠错。
- 实时持续纠错、多人会议分析、多语言、团队后台、声音克隆。
- 完整 AI 模拟对话、复杂游戏化，以及为使用 Agent 而增加的角色和流程。

## 9. 质量验证与成功信号

### 轻量内部检查

使用少量经同意的真实录音和受控声音对照，结合开发者及具备相应英语听辨能力的人的回听核对。只保留必要输入、输出和错误记录，不要求用户操作研究工具。

| 检查 | 预期行为 |
|---|---|
| 相同文字，改变重音或停顿 | 对目标变化给出相应且正确的声音观察，而非相同套话 |
| 相同声音，提供有错误的转写 | 不跟随错字编造声音问题；冲突无法解决时说明不确定 |
| 仅提供文字、移除音频 | 不输出有具体声音依据的发音或节奏诊断 |
| 相同音频重复比较、交换前后顺序 | 不虚构变化；观察不因版本标签产生方向性偏好 |
| 重说中仅改变目标声音特征 | 能识别该变化，不无故追加新的问题 |
| 自然停顿、清楚但有口音 | 不强行纠正，也不把口音当作能力问题 |
| 更流利但改变数字、否定或请求 | 提示内容变化，不直接奖励改善 |
| 参考示范与原句使用相同文字 | 人能听出所要求的声音区别；生成流畅不能代替目标达成 |

受控样本的实际声音差异需要先经回听确认；生成提示写了“加强重音”，不代表生成音频已经做到。保留未用于修改提示的样本检查泛化，并记录噪声、口音和设备相关失败。小样本用于查错和选择可交付能力，不用于宣称普遍准确率或长期学习效果。

### 三个主要成功信号

| 信号 | 记录方式 | 解释边界 |
|---|---|---|
| 听得有依据 | 逐项核对已展示建议的定位、声音观察和相关性；同时记录无问题样本误报及放弃判断情况 | 输出多、声明高置信度均不等于准确 |
| 用户练得明白 | 观察用户能否找到原音、感知区别、理解动作并独立尝试；记录卡住的位置 | 重说完成率以有可练建议的会话为分母，无建议会话单列 |
| 变化听得出来 | 核对原音与重说中目标变化是否存在，系统观察是否对应真实变化 | 同时报告符合方向、无变化、偏离方向、内容改变及无法判断，不能只选成功样本 |

同步记录首次可用反馈等待时间、示范等待时间、失败率和每次完成练习的实际成本。首批实测后确定分能力质量阈值；阈值未确定前只做受控试用，不默认开放未经核对的声音诊断。

重复使用、真实交流中的理解改善和长期迁移作为后续问题。需要证明效果时，再设计与“只回听并自行重说”的对照；不要求本次 MVP 先证明三个信息槽提高 10 个百分点。

## 10. 异常与结束路径

| 情况 | 用户可见行为 | 后续动作 |
|---|---|---|
| 音频可分析，但没有值得练的建议 | “这次没有找到需要优先练习的地方” | 自主回听、可选表达目标或结束；不要求重说 |
| 声音证据不足 | “这处我听得不够确定，暂时不给建议”并说明原因 | 扩大上下文、重录或结束；不暗示原表达已经清楚 |
| 噪声、多人、语言不匹配或内容不足 | 说明具体限制，不做能力评价 | 保留录音回听并引导重新输入 |
| 转写或定位不可靠 | 标记文字不确定；扩大到可播放的可靠范围 | 仍不能定位则隐藏该候选，不播放错片段或猜测精度 |
| 用户听不出区别或不同意 | 不重复坚持原结论 | 重新回听、放弃本条、换另一条或结束 |
| 重说含义改变或上下文不够 | 说明为何不能直接比较 | 确认改动、扩大范围或再说一次 |
| 重说无明显变化或偏离目标 | 给具体观察，不追加羞辱性评价或虚假鼓励 | 保留同一动作，再试或结束 |
| 分析、示范或比较失败 | 区分失败阶段，保留当前可用录音与结果 | 针对失败步骤重试，避免重复整套调用 |

“有建议并练习”“有建议但选择结束”“无建议”“证据不足或失败后结束”均是合法结束状态，分别记录，不全部计作学习完成或产品失败。

## 11. 隐私与非功能要求

- 首版仅面向本人主动录制的单人音频，不鼓励上传他人对话；录音前清楚披露第三方声音处理。
- 普通产品的音频、转写和结果只用于当前会话，结束或刷新后清理页面数据，不默认建立历史。服务端仅在处理所需期限内使用；超时、失败和取消也必须清理临时资源。
- 开发样本留存须单独同意，明确用途、期限和撤回方式。工作台不落盘不能被表述为所有第三方都零留存。
- 默认不将用户录音用于模型训练。实际供应商设置、合同及保留策略必须核实与产品承诺一致；若使用远端临时文件，处理后主动删除并处理删除失败。
- 不做声音身份、人格、智力、职业能力或心理状态推断，不把口音、音色或推测身份作为质量标准。
- 当前会话内避免重复转写和重复生成同一目标的同一参考；用户明确重试和必要复核除外。
- 初始性能目标：对 60 秒录音，从上传完成到首条可播放建议或明确无建议结果的 P95 不超过 30 秒；分别记录上传及示范等待。180 秒输入另行实测，不沿用短音频承诺。
- 关键录音、播放、目标选择和反馈支持键盘与屏幕阅读器；文字对比度至少 4.5:1，不能只靠颜色表达变化。
- 公开网络服务须具备访问控制、调用限额及失败重试保护；当前本地实现不等于已具备上线条件。

## 12. 验收与交付门槛

### 核心流程验收

1. 用户无需填写研究字段即可提交合格录音，推荐时长与硬上限明确，录音与上传遵守相同长度规则。
2. 有建议时首屏只有一个展开重点，能够立即回听对应原音、扩大上下文并理解一个练习动作。
3. 对首版启用的声音能力，参考示范经实际回听确认呈现目标差异；失败时不冒充已生成有效示范。
4. 用户能一键重说、正常速度比较原片段与重说；反复尝试保持当前目标，切换目标不会显示旧比较。
5. 系统只描述有声音依据的目标变化；内容不等价、未改变或无法判断时有对应行为，不强行宣布改善。
6. 用户可以隐藏提示、连着前后句说或直接结束；不要求每次完整重录，也不拿局部录音评价整段内容。
7. 无建议、证据不足、用户不同意及供应商失败均能自然结束或恢复，不迫使用户制造一个问题。
8. 结束会话能清理当前数据；内部留样与普通练习分离。

### 进入种子用户试用前

- 至少一类声音能力在独立检查样本上能完成“听见—示范—尝试—核对变化”；已知会编造证据、错误定位或误导示范的类别保持关闭。
- 第 9 节核心对照实际跑过并回听核对，保留失败记录；格式校验和代码测试通过不能代替声音检查。
- 目标用户能在无讲解帮助下完成核心任务；如听不出示范区别或不知道怎么尝试，应先修复该交互。
- 核心异常路径、会话清理和实测延迟可接受。通过这些条件表示可以试用，不表示已经证明长期教学效果或适合无约束公开发布。

## 13. 待验证决策与下一步

### 优先回答

1. 现有模型在哪一至两类声音现象上足够可靠？其错误是否来自文本暗示、定位还是听辨本身？
2. 独立听音再定位复听，相比当前联合输入是否有实际质量收益？额外等待是否值得？
3. 当前参考语音能否稳定示范指定的声音变化，而不只是生成另一段流畅语音？
4. 用户最常卡在找到重点、听出差别、理解动作还是自己说出来？
5. 何时需要扩大练习范围，何时一次局部尝试已足够？用户是否自然愿意再次使用？

### 实施顺序

1. 用真实声音及受控对照检查现有分析、定位、示范和变化识别，选定首批可交付能力。
2. 将普通入口收敛为原音与一个动作，移出研究工具；打磨回听、示范切换和一键重说。
3. 围绕同一目标实现具体变化反馈及上下文扩展，针对实际卡点迭代。
4. 核心闭环可用后再扩展能力、练习记忆与场景，并验证持续使用和真实沟通收益。

本轮的交付重点是一个可信、方便且能让用户听出变化的声音学习流程。
