# Oralign 开发计划

日期：2026-09-22。状态：D01–D04 已实现；D04 真人语音验收待完成，后续里程碑待执行。依据：[产品升级调研与方案](./product-strategy-2026-09-22.md)。

排期修订：Live 与声音证据从第 1 周同时开发，第 2 周完成首次联合验收。2026-09-23 已确定 Live 使用 Gemini Live，并完成 D01/D02；本计划的排期取代调研文档中的先后安排。

**交付目标**

交付可供目标用户连续使用的封闭 Alpha：用户进入场景开展 Live 对话，在自然停顿后查看带原音的反馈，进入一次针对性练习，得到同目标变化反馈，返回对话，并在下次打开时继续练习。

完整 Alpha 包含 Live、三类场景、声音证据、重说比较、历史和个人练习目标。首批可靠诊断覆盖停顿与节奏；重音、语调、音素分析按各自验证结果开放。保留当前单段录音/上传作为同等入口。

排期按两名持续投入的工程开发者、兼职产品/设计支持和可安排的语音评审估算：8–10 周。角色 A 偏前端、实时交互；角色 B 偏服务端、音频分析；这是工作分工建议，不表示已安排人员或启动并行代理。单人按 12–16 周调整。第一周完成技术试验后修正估算。

两条开发线各自交付可运行链路：A 负责 Live 的前后端接入，B 负责声音证据的提取、调度与证据卡。共同先约定采集与时间轴接口，随后每天集成。第二周交付一个场景内的双通路原型，第三至四周扩展场景与质量覆盖；总排期不因两线同时启动而直接减半。

**一、实现边界与默认决策**

| 项目 | 开发决策 |
|---|---|
| Web 应用 | 保留 Next.js/React 和现有视觉系统，新增 Live、回顾、成长页面 |
| 现有能力 | 复用 AudioPlayback、TranscriptExplorer、ReferencePractice、动作示范服务和供应商适配，不整站重写 |
| 状态管理 | 会话使用显式 reducer/状态机；避免继续向 Workbench.tsx 叠加互相依赖的 useState |
| Live 接入 | 使用 Gemini Live；已完成短期令牌、受约束 WebSocket 接入及无音频真实握手。后续验证语音往返、打断和时延 |
| 音频采集 | 新增实时采集/播放模块；现有完整录音上传链继续保留，不用反复上传完整音频模拟 Live |
| 证据分析 | 独立 Python worker/service，承担解码、声学测量、质量检测；Coach 负责解释而非生成测量值 |
| 数据 | 采用 PostgreSQL + 私有音频对象存储；使用数据访问接口，第三周开始持久化，具体托管部署在首周确定 |
| 作业 | 前两周本地单用户原型采用内存任务适配器 + worker，保持幂等、取消与版本校验；第三周接数据库任务记录与租约，进程重启前的临时作业不承诺恢复；暂不引入多套消息基础设施 |
| UI 数据更新 | 作业状态先使用有限频率轮询；轮询在离开会话/结束时停止。只有需要时再增加服务端事件推送 |
| 服务边界 | Next.js 承担短请求、授权、查询；长连接直连供应商或专用中继，声学作业由 worker 执行 |
| 访问控制 | 本地单用户原型可以先跑；首次邀请外部用户前，所有收费和数据端点必须有身份校验、归属校验与配额 |

实施 Next.js 改动前，按 `workbench/AGENTS.md` 阅读本机安装版本的相关指南。模型名称、SDK、短期凭据与流事件的具体协议在实现时重新核验官方文档。

**二、核心契约先定义，随后逐步实现**

数据对象：

- `Session`：场景、目标、反馈偏好、生命周期、供应商会话映射、是否保存录音。
- `Turn`：说话方、稳定 ID、起止时间、转写版本、实际播放范围、完成/中断状态。
- `AudioAsset`：归属用户/会话、音轨、采样率、时间偏移、时长、处理信息、保存/删除状态。
- `Evidence`：音频范围、测量值和单位、提取器版本、质量限制、观察、影响假设、候选/核实/撤回状态。
- `PracticeTarget`：来自哪条证据、具体动作、目标特征、比较条件。
- `Attempt`：原始与重说音频、内容是否可比、目标变化及无法判断原因。
- `SkillObservation`：来自哪个目标和尝试、所在场景、日期；不根据一次录音生成永久能力标签。
- `AnalysisJob`：输入版本、幂等键、阶段、运行租约、结果/错误、取消状态。

事件至少携带 `sessionId`、`turnId`（适用时）、`eventId`、时间轴坐标和版本。临时字幕不得作为稳定诊断输入；转写修订必须使依赖旧版本的候选重新核验。删除会话后，迟到作业不得重新写回结果。

连接状态与教学状态分开：连接为 idle/connecting/connected/reconnecting/ended/failed；交互为 listening/speaking/paused/reviewing/practicing。需要明确切换条件、麦克风所有权、播放清理和作业取消规则。

**三、里程碑与验收**

| 里程碑 | 时间与主要责任 | 开发内容 | 验收与展示 |
|---|---|---|---|
| M0：共同基础 | 第 1–2 天；A+B | 固定音频/轮次/证据契约与样本时间轴；约定本地任务接口和一组真实音频样本 | 同一音频能交给 Live 和分析两条路径，输出可映射回原音；后续开发不等待对方完整实现 |
| M1A：Live 开发线 | 第 1–2 周；A | Gemini Live 接入与短期凭据、开始/暂停/结束、字幕、打断、播放队列、转写轮次、基础断线处理；一个工作解释场景 | 完成 10 分钟真实对话；可插话；断线可恢复或明确重启；无重复播放/轮次；结束释放麦克风 |
| M1B：声音证据开发线 | 第 1–2 周；B，与 M1A 同期 | 真实音频解码、质量检测、VAD/能量、停顿测量、节奏统计、转写对齐；本地异步调度、候选复核、原音证据卡 | 在真实录音上复现测量，接入 Live 用户轮次；至少一类经过回听核对的声音观察可显示；原音与数值可追溯 |
| M2：联合完善 | 第 3–4 周；A+B | 三类场景、两种反馈时机、更多样本和设备条件、候选降级、上下文扩展；账号、持久化与可靠任务执行 | 证据与当前轮次不串用，分析失败不打断聊天；扩大质量覆盖并记录误报/弃权；外部试用前完成访问控制 |
| M3：对话内训练闭环 | 第 5–6 周；A+B | 证据→动作示范→重说→同目标比较→回到场景；内容保持检查、原音/重说 A/B；会话历史 | 连续完成全流程；目标切换不会串用结果；数字/否定等改变时不直接判进步；重新打开能查看选择保存的历史 |
| M4：成长与可靠交付 | 第 7–8 周；A+B | 个人目标、复习队列、新问题复测；删除和保留设置、配额、幂等和取消、弱网恢复、移动端和键盘交互 | 下次打开能继续目标；删除覆盖音频和派生结果；用户之间无数据访问串漏；失败仍能回听已完成内容 |
| M5：封闭试用与质量迭代 | 第 9–10 周；A+B+评审 | 10–20 人试用、质量回归、成本/时延分析；经验证的重音/语调或短句发音诊断实验 | 完成发布检查；形成问题与指标报告；未达标特征关闭诊断入口，保留其他已可用能力 |

第 2 周的联合演示必须包含“实时对话 → 当前用户轮次的实际声学分析 → 证据卡 → 点击回听”；只展示聊天或只展示离线分析均不算完成。第 4 周完善质量与可靠性，第 6 周展示完整训练闭环。测量现象和交流影响解释分别校验：测得间隔不等于证明表达有问题。

**四、建议按以下开发任务合入**

每项是可独立审查的工作包；体积过大时继续拆 PR，但保持依赖关系。每次合入都保留可运行主线，并用功能开关逐步开放。

| ID | 工作包 | 主要文件/新增位置（相对仓库根） | 依赖 | 完成条件 |
|---|---|---|---|---|
| D01（已完成） | 会话、音频与证据契约，状态 reducer | `workbench/lib/session/`、`lib/types.ts` | 无 | 事件去重、状态切换、目标/音频版本失效规则可测试 |
| D02（已完成） | Gemini Live 接入与真实握手 | `workbench/lib/live/`、`workbench/app/api/live/token/`、`workbench/docs/live-gemini-d01-d02.md` | D01 接口草案 | 短期令牌与受约束 WebSocket 的无音频真实握手通过；语音往返归 D03/D04 |
| D03（已完成，待 D04 真人验收） | 实时采集、缓冲和播放 | `workbench/hooks/useLiveAudio.ts`、`lib/audio/` | D01 | 统一采样时间轴；独立用户/AI 音轨；打断清队列；停止释放资源 |
| D04（已实现，待真人语音验收） | Live 会话端点、生命周期与 UI | `workbench/app/api/live/`、`app/live/page.tsx`、`components/live/` | D01–D03 | 可连续交谈、暂停、恢复；有身份/配额接入点与短期授权；本地之外不得开放裸端点 |
| D05 | 三类场景、反馈偏好和首页入口 | `workbench/lib/scenarios/`、`app/page.tsx`、`components/live/` | D04 | 三类场景有角色、信息和目标；自由聊天可用；现有录音入口仍可用 |
| D06 | 身份、存储、任务记录与权限 | `workbench/lib/server/`、数据库迁移、`app/api/sessions/` | D01；与 D03–D05 可并行 | 收费路由与数据端点有授权、归属与限额；音频私有；作业可取消、去重 |
| D07 | 声学分析 worker 与样本基线 | `services/audio-analysis/`、`evaluation/` | D01；与 D02–D04 同时启动，不依赖 D06 | 输入真实音频→版本化测量输出；低质量能弃权；有人工核对样本 |
| D08 | 轮次分析调度与候选复核 | `workbench/lib/evidence/`、`lib/providers.ts`、`lib/prompts.ts`、`app/api/analysis-jobs/` | D01、D07；先以真实录音回放产生轮次，再接 D03/D04；持久化后接 D06 | 第一轮即有异步分析；复听定位；无效单项隔离；过期结果不展示 |
| D09 | 证据卡、时间轴与回顾 | `components/evidence/`、`app/review/[sessionId]/page.tsx`；复用播放器 | D01 接口先行开发；第二周接 D08 和 Live | 第二周可从 Live 卡片回听真实证据；随后补上下文扩展与会后推荐；默认最多展开一个重点 |
| D10 | 精练流程与同目标比较 | `components/practice/`、`app/api/attempts/`、`lib/comparison/` | D07、D09；复用现有 TTS | 内容可比后判断目标变化；支持无变化/偏离/无法判断；取消、切目标不串结果 |
| D11 | 回到对话与新问题复测 | `lib/session/`、`components/live/`、`components/practice/` | D10 | 练完恢复角色和上下文；新问题只跟踪原目标，不自动追加纠错 |
| D12 | 历史、个人目标与复习 | `app/history/`、`app/progress/`、`lib/practice-plan/` | D06、D10 | 目标都有证据出处；可继续/忽略/删除；一次成功不显示为长期掌握 |
| D13 | 运行可靠性和数据生命周期 | 横跨 session、storage、worker、API 与 UI | D04、D06 起增量开展，M4 收口 | 弱网、超时、配额、重复提交、删除与迟到写入全部验证 |
| D14 | 留出集评估与用户试用 | `evaluation/`、`workbench/docs/alpha-validation.md` | D07 起持续，完整试用依赖 D11–D13 | 声音质量、用户任务、成本与时延分别报告；明确可开放特征 |

Live 路径：D01 → D02/D03 → D04。声音证据路径：D01 → D07 → D08，D09 根据契约同步开发。两条路径第二周在 D04 + D08 + D09 汇合，再进入 D10 → D11。D06 的持久化不阻塞本地两线集成，但必须在外部试用前完成；D12 在比较契约稳定后推进，D13/D14 全程开展。

共享接口以同一条用户音轨为输入，一份采集同时送往 Live 和分析缓冲区；分析沿用自己的质量/VAD判断，供应商的轮次切分只作导航信号。前期回放器发送真实录音及对应事件，使 B 不必等 Live 连接稳定。模拟数据只用于界面和事件测试，不作为声音证据验收。

上述目录和端点是拟议位置，不代表当前已存在。旧 `WorkbenchSession`、`Friction` 通过兼容适配迁移；旧录音入口完成新契约接入后再移除 `intent_slot`、未校准的证据等级等旧字段，避免一次破坏全部页面。

**五、第一轮开发：10 个工作日**

| 时间 | A：Live 开发线 | B：声音证据开发线 | 联合产出 |
|---|---|---|---|
| 第 1–2 天 | 与 B 固定契约；会话 reducer、Live 页面骨架、Gemini Live 接入 | 与 A 固定时间轴；真实样本、音频回放器、解码和质量检测 | 一条真实语音往返；同一真实音频能送入分析并定位 |
| 第 3–4 天 | 实时采集/播放、会话端点、短期凭据、用户轮次事件 | VAD/能量与停顿测量、节奏统计、转写对齐、版本化测量输出 | 实际麦克风音轨进入两个处理路径；声学结果可以回放核对 |
| 第 5–6 天 | 打断、暂停、字幕、清理播放；Live 侧挂载证据面板 | 异步调度、候选复核、证据卡与原音播放、结果版本校验 | Live 轮次结束后可看到实际测量的声音观察，聊天继续 |
| 第 7–8 天 | 工作解释场景、反馈时机切换、基础断线恢复 | 与原音逐条核对，补低质量弃权、无效候选隔离、迟到结果处理 | 同场景完成边聊边分析与回听；错误分析不破坏对话 |
| 第 9–10 天 | 桌面/手机交互与时延修复 | 声音样本回归、误报/覆盖率记录、分析耗时与成本检查 | 10 分钟 Live + 声音证据联合演示、问题与质量记录 |

第一轮退出标准：能够进行多轮对话，支持打断/暂停/结束；至少一类真实测量的声音观察绑定正确轮次和原音；卡片可回听核对；无可靠证据时不生成诊断；对话不会等待分析；拒绝麦克风、分析失败或连接失败有明确处理。初期只对通过核验的声音现象作描述，涉及交流影响的建议还需相应复核，不把原型验收当成最终质量达标。

**六、测试与发布门槛**

自动化测试集中在高风险逻辑：事件乱序/重复、音频偏移与裁切、打断播放、内容比较、结果失效、访问权限、取消和删除。静态文案或简单样式不另写镜像测试。

| 层级 | 必做验证 |
|---|---|
| 工程回归 | 现有测试、typecheck、lint、build；新状态机和契约测试；worker 单元/集成测试 |
| 真实设备 | 桌面 Chrome、Android Chrome、iOS Safari；拒绝麦克风、蓝牙切换、后台挂起、扬声器回声、网络中断；不承诺后台持续录音 |
| 声音证据 | 同文本不同声音、同声音错误字幕、无问题但有口音、低音量/噪声、ASR 漏词；按说话者划分开发/留出样本 |
| 重说比较 | 同音频不能判进步；A/B 顺序交换一致；重要内容改变则判不可比；目标切换使旧比较失效 |
| 数据生命周期 | 用户间隔离、私有对象下载、删除后不可读、作业迟到不恢复数据、取消与有限重试不会重复扣配额 |
| 产品任务 | 无引导完成对话→证据→练习→返回对话→下次继续；无建议和判断不足分支也能结束 |

性能目标沿用调研建议，属于待验证预算：确认用户结束发言到首段可听回复 P50 ≤0.8 秒/P95 ≤1.8 秒；打断后停播 P95 ≤300 毫秒；可用证据卡在轮次确定后约 2–5 秒出现。单独记录用户最后语音至回复的总等待和错误抢话，避免靠过早截断用户达标。分析超时自动转入会后回顾。

质量门槛：首批展示的声音观察，在留出集上至少 90% 获人工回听支持，同时报告覆盖率、误报、漏检和样本量；不能用全部弃权换取表面准确率。该门槛不代表已经证明学习效果，也不是小样本对总体的统计保证。未通过的特征关掉诊断，不阻塞通过验收的 Live 和其他能力。

每个工作包的完成要求：交互完整，相关错误状态可用，关键验证通过，记录真实外部调用限制，有功能关闭/回退方式。发布前必须经过真实语音调用；模拟供应商结果只能证明程序流程。

**七、范围调整与风险处理**

| 风险 | 开发处理 |
|---|---|
| 二语思考被误判为说完 | 调整轮次策略，提供“我还没说完”/按住说话；同时记录抢话率和回复时延 |
| Live 支持好但诊断差 | 保持实时对话，关闭对应诊断；证据路径独立改进 |
| 时间对齐漂移 | 使用样本位置/统一时间轴和版本；无法稳定定位时扩大上下文或弃权 |
| ASR 带偏发音判断 | 声学测量独立，转写修订后复核；音素分析先限定为经验证的短句精练 |
| 过多反馈影响表达 | 默认轮后标记；进入教学由用户选择；会话摘要筛选少量重点 |
| 重复分析导致成本上升 | 一轮一次基础分析，重任务只处理候选片段；示范按需；设单会话预算与空闲断开 |
| 排期超出 | 先延后重音/语调/音素扩展和精细趋势图；保留 Live、证据、精练、基础历史与恢复这一完整旅程 |

D01–D04 已实现：D04 页面完成真实无音频 Gemini 握手，生命周期、转写、打断与本地访问边界有自动化测试。真人麦克风语音往返仍需按 D04 文档验收。下一批开发从 D07–D09 推进；首个可演示目标是能在真实 Live 对话中生成并回听声音证据的联合版本。
